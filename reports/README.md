Generated by `scripts/greptile-report.mjs` — one `<pr-number>.json` per PR, keyed to the node IDs in `blast-radius.html`.
Regenerate locally with `GITHUB_TOKEN=... node scripts/greptile-report.mjs <pr>`.
